export default class Produto {

  //Modelo de produto
  constructor(id, nome, descricao, preco) {
    this.id = id;
    this.nome = nome;
    this.descricao = descricao;
    this.preco = preco;
  }
}
